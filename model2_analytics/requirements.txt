opencv-python-headless==4.9.0.80
numpy==1.26.4
httpx==0.27.0
asyncpg==0.29.0
# Pinned to the exact same version as model1-registry/requirements.txt
# (2.0.52), not the version originally pinned here (2.0.30). Both
# requirements.txt files get installed into the SAME environment (see
# infra/Dockerfile: `pip install -r requirements.txt
# -r requirements-model2.txt`, since model2's routers run inside
# model1's process per finding 5's main.py dynamic loader) -- two
# different exact pins of the same package across the two files makes
# that combined install fail outright with a modern pip resolver
# (verified locally). See AuditReport2.md finding 7.
sqlalchemy[asyncio]==2.0.52
pydantic==2.9.0
# facenet-pytorch pulls in its own torch (<2.3.0,>=2.2.0 per its own pin) as
# a transitive dependency -- a different, older torch than the one pinned
# in pipeline/requirements.txt (2.14.0), which is fine since the two never
# install into the same environment (see infra/Dockerfile: only this file
# is installed into the app container; pipeline/requirements.txt is for
# offline training only). See AuditReport2.md finding 7.
facenet-pytorch==2.6.0
