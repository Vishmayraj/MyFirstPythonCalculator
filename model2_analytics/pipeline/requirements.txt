# Model 2 — AI Pipeline Dependencies
# Install with: pip install -r requirements.txt
#
# All versions pinned exactly (AuditReport2.md finding 7) -- this tree is
# heavier and more update-sensitive than model1's (torch/torchvision/
# ultralytics can change model behavior or break compatibility between
# releases, not just introduce a security drift). torch==2.14.0 and
# torchvision==0.29.0 are pinned together deliberately: torchvision 0.29.0
# requires exactly torch==2.14.0, so bump them as a pair, not independently.

# Core YOLO26m
ultralytics==8.4.142
torch==2.14.0
torchvision==0.29.0

# Computer Vision
opencv-python==5.0.0.93
Pillow==12.3.0
numpy==2.5.2

# Dataset
datasets==5.0.1
huggingface-hub==1.30.0

# Evaluation & Visualization
matplotlib==3.11.1
seaborn==0.13.2
scikit-learn==1.9.0
pandas==3.0.5

# Utilities
tqdm==4.70.0
PyYAML==6.0.3
requests==2.34.2
